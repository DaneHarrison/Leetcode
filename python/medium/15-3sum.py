def threeSum(self, nums: list[int]) -> list[list[int]]:
    seen = {}

    for i in range(len(nums) -1):
        for j in range(i + 1, len(nums)):
            
    for i in nums:
        for j in nums